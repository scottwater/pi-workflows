{
  "name": "pi-workflows",
  "version": "0.1.0",
  "description": "Named slash-command workflows for launching pi-subagents agents and simple workflow compositions.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.ts"
  },
  "keywords": [
    "pi-package",
    "pi",
    "pi-workflows",
    "pi-subagents"
  ],
  "pi": {
    "extensions": [
      "./extensions"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "@earendil-works/pi-tui": "^0.74.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "@earendil-works/pi-tui": "^0.74.0"
  }
}
